services:
  claude-deck:
    build: .
    ports:
      - "${PORT:-3011}:3011"
    volumes:
      - ${HOME}/.claude:/root/.claude:ro
      - claude-deck-data:/root/.claude-deck
    environment:
      - PORT=3011
    restart: unless-stopped

volumes:
  claude-deck-data:
