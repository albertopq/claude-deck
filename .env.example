# Server Configuration
PORT=3011

# Database (optional, defaults to ~/.claude-deck/data.db)
# DB_PATH=~/.claude-deck/data.db

# SSH config for "Open in VS Code" remote button (optional)
# SSH_HOST=myserver.example.com
# SSH_PORT=22
